# archiso live-media hooks for the ISO initramfs
# installed targets use profile/mkinitcpio.conf.lamx instead, the installer
# removes this file so normal boots never see live-media hooks
HOOKS=(base udev microcode modconf kms memdisk archiso archiso_loop_mnt block filesystems keyboard keymap consolefont)
