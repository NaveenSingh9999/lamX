[Unit]
Description=KZC weekly integrity check
RefuseManualStop=yes

[Service]
Type=oneshot
ExecStart=/bin/sh -c 'command -v aide >/dev/null || exit 0; aide --check > /var/log/kzc/aide-report.txt 2>&1 || logger -t kzc-aide "AIDE differences found, see report"'
