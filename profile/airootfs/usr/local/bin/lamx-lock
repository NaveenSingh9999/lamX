#!/usr/bin/env bash
# lock - xfce screensaver lock, falls back to session lock
if command -v xfce4-screensaver-command >/dev/null; then
  exec xfce4-screensaver-command --lock
else
  loginctl lock-session
fi
